{
    "eclecticiq_url": {
        "description": "the url of the EclecticIQ instance to use",
        "required": true,
        "type": "string"
    },
    "password": {
        "description": "the EclecticIQ user's password",
        "required": true,
        "secret": true,
        "type": "string"
    },
    "ssl_verify": {
        "default": true,
        "description": "whether to verify ssl certificates or not",
        "required": false,
        "type": "boolean"
    },
    "username": {
        "description": "the EclecticIQ user",
        "required": true,
        "type": "string"
    }
}
