{
    "description": "Perform Baseline Information Gathering on Specific Client Group",
    "enabled": true,
    "entry_point": "run.py",
    "name": "do_baseline_by_group",
    "parameters": {
        "args": {
            "default": "['group_ids']",
            "description": "the argument names",
            "immutable": true,
            "type": "string"
        },
        "body": {
            "default": "",
            "description": "the request body to use",
            "immutable": true,
            "type": "string"
        },
        "body_args": {
            "default": "[]",
            "description": "the body argument names",
            "immutable": true,
            "type": "string"
        },
        "endpoint": {
            "default": "/command-queue/baseline?group_ids={}",
            "description": "the endpoint to use",
            "immutable": true,
            "type": "string"
        },
        "group_ids": {
            "description": "the ids of the groups to preform baseline information gathering on",
            "required": true,
            "type": "string"
        },
        "header": {
            "default": "{'Content-Type': 'application/json'}",
            "description": "the header to use",
            "immutable": true,
            "type": "string"
        },
        "method": {
            "default": "POST",
            "description": "the HTTP method to use",
            "immutable": true,
            "type": "string"
        }
    },
    "runner_type": "python-script"
}
