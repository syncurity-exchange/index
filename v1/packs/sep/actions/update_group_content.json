{
    "description": "Update Content on Specific Group",
    "enabled": true,
    "entry_point": "run.py",
    "name": "update_group_content",
    "parameters": {
        "args": {
            "default": "['group_ids']",
            "description": "the argument names",
            "immutable": true,
            "type": "string"
        },
        "body": {
            "default": "",
            "description": "the request body to use",
            "immutable": true,
            "type": "string"
        },
        "body_args": {
            "default": "[]",
            "description": "the body argument names",
            "immutable": true,
            "type": "string"
        },
        "endpoint": {
            "default": "/command-queue/updatecontent?group_ids={}",
            "description": "the endpoint to use",
            "immutable": true,
            "type": "string"
        },
        "group_ids": {
            "description": "the ids of the groups to update content on",
            "required": true,
            "type": "string"
        },
        "header": {
            "default": "{'Content-Type': 'application/json'}",
            "description": "the header to use",
            "immutable": true,
            "type": "string"
        },
        "method": {
            "default": "POST",
            "description": "the HTTP method to use",
            "immutable": true,
            "type": "string"
        }
    },
    "runner_type": "python-script"
}
