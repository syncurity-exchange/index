{
    "description": "Delete a Symantec Endpoint Protection 14 Manager Client Group",
    "enabled": true,
    "entry_point": "run.py",
    "name": "delete_group",
    "parameters": {
        "args": {
            "default": "['group_ids']",
            "description": "the argument names",
            "immutable": true,
            "type": "string"
        },
        "body": {
            "default": "",
            "description": "the request body to use",
            "immutable": true,
            "type": "string"
        },
        "body_args": {
            "default": "[]",
            "description": "the body argument names",
            "immutable": true,
            "type": "string"
        },
        "endpoint": {
            "default": "/groups/{}",
            "description": "the endpoint to use",
            "immutable": true,
            "type": "string"
        },
        "group_ids": {
            "description": "the ids of the groups to delete",
            "required": true,
            "type": "string"
        },
        "header": {
            "default": "{'Content-Type': 'application/json'}",
            "description": "the header to use",
            "immutable": true,
            "type": "string"
        },
        "method": {
            "default": "DELETE",
            "description": "the HTTP method to use",
            "immutable": true,
            "type": "string"
        }
    },
    "runner_type": "python-script"
}
