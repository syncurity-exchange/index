{
    "description": "Location information for a specific Symantec Endpoint Protection 14 Client Group",
    "enabled": true,
    "entry_point": "run.py",
    "name": "get_group_locations",
    "parameters": {
        "args": {
            "default": "['group_ids']",
            "description": "the argument names",
            "immutable": true,
            "type": "string"
        },
        "body": {
            "default": "",
            "description": "the request body to use",
            "immutable": true,
            "type": "string"
        },
        "body_args": {
            "default": "[]",
            "description": "the body argument names",
            "immutable": true,
            "type": "string"
        },
        "endpoint": {
            "default": "/groups/{}/locations",
            "description": "the endpoint to use",
            "immutable": true,
            "type": "string"
        },
        "group_ids": {
            "description": "the group ids to get location information for",
            "required": true,
            "type": "string"
        },
        "header": {
            "default": "{'Content-Type': 'application/json'}",
            "description": "the header to use",
            "immutable": true,
            "type": "string"
        },
        "method": {
            "default": "GET",
            "description": "the HTTP method to use",
            "immutable": true,
            "type": "string"
        }
    },
    "runner_type": "python-script"
}
