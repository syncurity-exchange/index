{
    "description": "Assign User to Alert in IR-Flow",
    "enabled": true,
    "entry_point": "assign_user_to_alert.py",
    "name": "assign_user_to_alert",
    "parameters": {
        "alert_num": {
            "description": "Alert number to attach to incident",
            "required": true,
            "type": "integer"
        },
        "credential_name": {
            "description": "Name of credentials from this pack's configuration to use, which includes hostname and api creds.",
            "required": true,
            "type": "string"
        },
        "username": {
            "description": "The username of the IR-Flow user to assign this alert to",
            "required": true,
            "type": "string"
        }
    },
    "runner_type": "python-script"
}
