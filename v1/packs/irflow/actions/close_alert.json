{
    "description": "Close an Alert in IR-Flow",
    "enabled": true,
    "entry_point": "close_alert.py",
    "name": "close_alert",
    "parameters": {
        "alert_num": {
            "description": "IR-Flow Alert Number to close",
            "required": true,
            "type": "integer"
        },
        "close_reason": {
            "description": "Alert Close Reason",
            "required": true,
            "type": "string"
        },
        "credential_name": {
            "description": "Name of credentials from this pack's configuration to use, which includes hostname and api creds.",
            "required": true,
            "type": "string"
        }
    },
    "runner_type": "python-script"
}
