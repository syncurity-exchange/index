{
    "description": "Retrieve the current data in the specified fact group",
    "enabled": true,
    "entry_point": "get_fact_group.py",
    "name": "get_fact_group",
    "parameters": {
        "credential_name": {
            "description": "Name of credentials from this pack's configuration to use, which includes hostname and api creds.",
            "required": true,
            "type": "string"
        },
        "fact_group_id": {
            "description": "The IR-Flow assigned ID of the fact_group to GET",
            "required": true,
            "type": "integer"
        }
    },
    "runner_type": "python-script"
}
