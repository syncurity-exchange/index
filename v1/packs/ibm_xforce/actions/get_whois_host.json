{
    "description": "Returns a JSON object containing information about the given host\naddress.",
    "enabled": true,
    "entry_point": "run.py",
    "name": "get_whois_host",
    "parameters": {
        "body_args": {
            "default": "[]",
            "description": "the body argument names",
            "immutable": true,
            "type": "string"
        },
        "endpoint": {
            "default": "/whois/{host}",
            "description": "the endpoint to use",
            "immutable": true,
            "type": "string"
        },
        "host": {
            "description": "Search term, For example, ibm.com, 8.8.8.8",
            "required": true,
            "type": "string"
        },
        "method": {
            "default": "GET",
            "description": "the HTTP method to use",
            "immutable": true,
            "type": "string"
        }
    },
    "runner_type": "python-script"
}
