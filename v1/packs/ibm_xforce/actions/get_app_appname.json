{
    "description": "Returns the Internet Application Profiles (IAP) that needs to be\nfetched.",
    "enabled": true,
    "entry_point": "run.py",
    "name": "get_app_appname",
    "parameters": {
        "appname": {
            "description": "Search term, For example, TestApp for Social Networking",
            "required": true,
            "type": "string"
        },
        "body_args": {
            "default": "[]",
            "description": "the body argument names",
            "immutable": true,
            "type": "string"
        },
        "endpoint": {
            "default": "/app/{appname}",
            "description": "the endpoint to use",
            "immutable": true,
            "type": "string"
        },
        "method": {
            "default": "GET",
            "description": "the HTTP method to use",
            "immutable": true,
            "type": "string"
        }
    },
    "runner_type": "python-script"
}
