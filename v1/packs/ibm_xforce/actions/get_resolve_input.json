{
    "description": "Returns live and passive DNS records.",
    "enabled": true,
    "entry_point": "run.py",
    "name": "get_resolve_input",
    "parameters": {
        "body_args": {
            "default": "[]",
            "description": "the body argument names",
            "immutable": true,
            "type": "string"
        },
        "endpoint": {
            "default": "/resolve/{input}",
            "description": "the endpoint to use",
            "immutable": true,
            "type": "string"
        },
        "input": {
            "description": "ip or domain or url",
            "required": true,
            "type": "string"
        },
        "method": {
            "default": "GET",
            "description": "the HTTP method to use",
            "immutable": true,
            "type": "string"
        }
    },
    "runner_type": "python-script"
}
