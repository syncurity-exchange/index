{
    "description": "Returns an API key and password pair, which are needed when accessing\nthe API as an logged in user.",
    "enabled": true,
    "entry_point": "run.py",
    "name": "post_auth_api_key",
    "parameters": {
        "body_args": {
            "default": "['name']",
            "description": "the body argument names",
            "immutable": true,
            "type": "string"
        },
        "endpoint": {
            "default": "/auth/api_key",
            "description": "the endpoint to use",
            "immutable": true,
            "type": "string"
        },
        "method": {
            "default": "POST",
            "description": "the HTTP method to use",
            "immutable": true,
            "type": "string"
        },
        "name": {
            "description": "name of API key to be generated",
            "required": false,
            "type": "object"
        }
    },
    "runner_type": "python-script"
}
