{
    "description": "Indicates whether or not a domain is monitored.",
    "enabled": true,
    "entry_point": "run.py",
    "name": "get_actions_monitor",
    "parameters": {
        "body_args": {
            "default": [],
            "description": "the body argument names",
            "immutable": true,
            "type": "array"
        },
        "endpoint": {
            "default": "/actions/monitor",
            "description": "the endpoint to use",
            "immutable": true,
            "type": "string"
        },
        "method": {
            "default": "GET",
            "description": "the HTTP method to use",
            "immutable": true,
            "type": "string"
        },
        "query": {
            "description": "domain for which to check for monitoring",
            "required": true,
            "type": "string"
        },
        "query_args": {
            "default": [
                "query"
            ],
            "description": "the query argument names",
            "immutable": true,
            "type": "array"
        }
    },
    "runner_type": "python-script"
}
