{
    "description": "Disable a user in MSGraph",
    "enabled": true,
    "entry_point": "update_user.py",
    "name": "disable_user",
    "parameters": {
        "action": {
            "default": "disable",
            "immutable": true,
            "required": true,
            "type": "string"
        },
        "user_guid": {
            "description": "A UPN or uid for the user to disable",
            "required": true,
            "type": "string"
        }
    },
    "runner_type": "python-script"
}
