{
    "description": "Add a User to a Group via MSGraph API",
    "enabled": true,
    "entry_point": "add_user_to_group.py",
    "name": "add_user_to_group",
    "parameters": {
        "group_id": {
            "description": "Group ID",
            "required": true,
            "type": "string"
        },
        "user_id": {
            "description": "User ID",
            "required": true,
            "type": "string"
        }
    },
    "runner_type": "python-script"
}
